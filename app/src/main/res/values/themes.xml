<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.M2MMusic" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
<!--        <item name="colorPrimary">@color/keshizumi</item>-->
        <item name="colorPrimary">@color/white</item>
<!--        <item name="android:textColorPrimary">@color/keshizumi_text</item>-->
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">#3390c2</item>
        <item name="colorSecondaryVariant">#3390c2</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorAccent">#f8e578</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Customize your theme here. -->
        <!-- 底部小白条透明 -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="toolbar_title_textStyle">
        <item name="android:textSize">18sp</item>
        <item name="android:fontFamily">@font/zhuzia_sleek</item>
    </style>

    <style name="toolbar_collapsing_title_textStyle">
        <item name="android:fontFamily">@font/zhuzia_sleek</item>
    </style>

    <style name="toolbar_expend_title_textStyle">
        <item name="android:textSize">22sp</item>
        <item name="android:fontFamily">@font/zhuzia_sleek</item>
    </style>

    <style name="bottom_activity_bg_style" parent="Theme.M2MMusic">
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上，如果设置为true则类似于展示成dialog，所以界面左右两边会有空边 -->
        <item name="android:windowIsFloating">false</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 模糊 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 遮罩层 -->
        <item name="android:backgroundDimAmount">0.5</item>
    </style>
</resources>